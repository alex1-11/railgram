<p>
  <strong><%= @post.user.name %> </strong>
  <%= @post.caption %>
</p>

<h2>Comments</h2>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render comment if comment.id %>
  <% end %>
</div>

<%= render 'form', comment: @new_comment %>
