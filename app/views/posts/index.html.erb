<br/><br/>

<%= link_to "New post", new_post_path %>

<h1>
  <% if @user == current_user %>
    My posts:
  <% else %>
    <%= @user.name %> posted:
  <% end %>
</h1>

<div id="posts">
  <% @posts.each do |post| %>
    <%= render post %>
    <p>
      <%= link_to "Show this post", post_path(post) %>
    </p>
  <% end %>
</div>
