<div class='bg-body-tertiary rounded-3 shadow my-5 px-0 py-3'>
  <%= render partial: 'users/profile', locals: { user: @user } %>
</div>

<div id="posts" class='mt-5'>
  <% @posts.each do |post| %>
    <%= render partial: 'posts/post',
           locals: { post:,
                     likes: @likes } %>
  <% end %>
</div>
