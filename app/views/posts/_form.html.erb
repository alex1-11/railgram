<div class='row justify-content-center text-center'>
  <%= form_with(model: post, url: url_for(post), html: { class: 'col-sm-8 col-md-6' }) do |form| %>
    <% if post.errors.any? %>
      <div class="alert alert-danger alert-dismissible fade show">
        <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

        <ul>
          <% post.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>

    <div>
      <%= image_tag post.image_url(:post_size) if post.id %>
    </div>

    <div class="input-group mb-3">
      <%= form.label :image, 'Image', class: 'input-group-text col-3' %>
      <%= form.hidden_field :image, value: post.cached_image_data, id: nil %>
      <%= form.file_field :image, class: 'form-control' %>
    </div>

    <div class="input-group mb-3">
      <span class='input-group-text col-3'>Caption</span>
      <%= form.text_area :caption, class: 'form-control' %>
    </div>

    <div>
      <%= form.submit %>
    </div>
  <% end %>
</div>
