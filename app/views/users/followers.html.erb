<%= link_to "Back", :back %>

<div id='followers'>
  <ul>
    <% @followers.each do |follower| %>
      <li>
        <%= link_to follower.name, user_path(follower) %>
      </li>
    <% end %>
  </ul>
</div>
